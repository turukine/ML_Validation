Validation Project: Методы валидации, отбора признаков и оптимизации гиперпараметров
Этот проект посвящён изучению и практической реализации методов валидации, отбора признаков и оптимизации гиперпараметров в машинном обучении на примере данных по аренде квартир в Нью-Йорке.

📁 Структура проекта
ML3_Validation.ipynb – основной ноутбук с кодом, экспериментами и выводами.

data/train.json – обучающие данные (список квартир с признаками и уровнем интереса).

data/test.json – тестовые данные.

README.md – описание проекта и результатов.

🎯 Цели проекта
Изучить теоретические основы методов валидации.

Реализовать различные методы разбиения данных:

Случайное разбиение (2 и 3 части)

Разбиение по дате (2 и 3 части)

K-Fold, Grouped K-Fold, Stratified K-Fold, Time Series Split

Сравнить собственные реализации с методами из sklearn.

Изучить и применить методы отбора признаков:

Lasso (встроенный отбор)

Простой отбор по NaN и корреляции

Permutation Importance

SHAP (Shapley values)

Оптимизировать гиперпараметры модели ElasticNet с помощью:

Grid Search

Random Search

Bayesian Optimization (Optuna)

Сравнить эффективность разных подходов и выбрать лучшие практики.

📈 Данные
Данные взяты из соревнования RentHop (прогнозирование уровня интереса к квартирам).
Целевая переменная – interest_level (0 – low, 1 – medium, 2 – high).
Признаки включают:

Числовые: bathrooms, bedrooms

Бинарные (из текстового поля features): Elevator, HardwoodFloors, Doorman, Dishwasher и др. (20 признаков)

🔧 Основные этапы
1. Предобработка данных
Загрузка JSON-файлов.

Преобразование целевой переменной в числовой формат.

Извлечение бинарных признаков из текстового поля features.

2. Реализация методов разбиения данных
Случайное разбиение (60/20/20 и 80/20).

Разбиение по дате создания объявления (created).

K-Fold, Grouped K-Fold, Stratified K-Fold, Time Series Split.

3. Сравнение с sklearn
Собственные реализации показали идентичное поведение с sklearn:

K-Fold: совпадение по количеству фолдов и размеру выборок.

Stratified K-Fold: сохранение распределения классов.

Time Series Split: хронологическое разбиение без утечки данных.

4. Отбор признаков
Применены 4 метода:

Lasso coefficients – отбор по абсолютным значениям коэффициентов.

Simple selection – комбинация NaN-ratio и корреляции с целевой переменной.

Permutation Importance – оценка влияния признака через перестановку значений.

SHAP – объяснение важности признаков через Shapley values.

Результаты: Все методы показали схожую эффективность (MSE ≈ 0.394), но Lasso coefficients был выбран как эталонный из-за простоты интерпретации.

5. Оптимизация гиперпараметров (ElasticNet)
Сравнивались:

Grid Search – полный перебор (25 комбинаций).

Random Search – случайный перебор (25 итераций).

Optuna (Bayesian Optimization) – 50 итераций.

Optuna с кросс-валидацией – 30 итераций с K-Fold.

Лучший метод: Optuna (без CV) нашла оптимальные параметры alpha=0.0021, l1_ratio=0.147 за 50 итераций, показав Test RMSE = 0.6189.

📊 Ключевые результаты
Метод валидации	MSE (mean ± std)
K-Fold	0.3920 ± 0.0067
Stratified K-Fold	0.3919 ± 0.0003
Time Series Split	0.3918 ± 0.0085
Метод отбора признаков	Test MSE
Lasso coefficients	0.3941
Simple selection	0.3941
Permutation Importance	0.3941
SHAP	0.3941
Метод оптимизации	Test RMSE	Итераций
Grid Search	0.6189	25
Random Search	0.6189	25
Optuna	0.6189	50
Optuna с CV	0.6189	30
📌 Выводы
Time Series Split оказался лучшей схемой валидации для данных с временной зависимостью.

Методы отбора признаков дали схожие результаты, но Lasso наиболее интерпретируем и быстро вычисляется.

Optuna показала себя как эффективный инструмент оптимизации гиперпараметров, превосходящий Grid Search по скорости сходимости.

Разница между качеством на валидации и тесте минимальна, что говорит об отсутствии переобучения.

🚀 Как запустить проект
Установите зависимости:

bash
pip install numpy pandas scikit-learn shap optuna
Поместите файлы train.json и test.json в папку data/.

Откройте и выполните ячейки в ML3_Validation.ipynb.

📚 Автор
Проект выполнен в рамках изучения методов валидации и оптимизации моделей машинного обучения.
Реализованы как базовые алгоритмы, так и сравнение с современными библиотеками (sklearn, shap, optuna).
